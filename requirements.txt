# Dependencias para ongoing-bps-state-short-term
log-distance-measures==2.1.0
pix-framework==0.14.2
ongoing-process-state==2.1.2

# Prosimos se instala desde repo clonado
# IMPORTANTE: Configura la ruta en configs/config.yaml (external_repos.prosimos_path)
# Luego instala con:
#   pip install -e /ruta/a/tu/Prosimos
# 
# O descomenta y ajusta la siguiente línea con tu ruta local:
# -e /ruta/a/tu/Prosimos
# 
# Ejemplo de estructura esperada:
#   paper1/repos-asis-online-predictivo/.../libraries-used/Prosimos
pandas==2.2.3
numpy==1.26.4
networkx==3.4.2
lxml==5.3.1
scikit-learn==1.6.1
tqdm==4.67.1
pyyaml>=6.0
pm4py>=2.7.0  # Para soporte de archivos XES

